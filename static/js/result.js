/*
 * result.html은 읽기 전용 페이지라 필수 JS 로직은 없습니다.
 * 구조 일관성(html/css/js 분리)을 위해 파일만 유지하고,
 * 추후 기능(예: 피드백 복사, 다시 풀기 버튼 등)을 추가할 때 여기에 넣으면 됩니다.
 */

document.addEventListener("DOMContentLoaded", () => {
  // 현재는 초기화할 인터랙션이 없습니다.
});
